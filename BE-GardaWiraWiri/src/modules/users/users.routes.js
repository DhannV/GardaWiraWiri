'use strict';

const { Router } = require('express');

const router = Router();

// Routes untuk module "users" akan diimplementasikan pada tahap berikutnya.
// Referensi endpoint ada di dokumentasi perancangan API.

module.exports = router;
